## Critical finding before the table

The four "recently active" branches you flagged — **origin/jinjian, origin/yu-cao, origin/meta-diffusion-clean, origin/jing-peng** — are **not part of this paper's codebase at all**. They are orphan git histories (no merge-base with `main`, root commit `449ec51 "Meta-Diffusion: few-shot diffusion adaptation via a low-dimensional coordinate"`) belonging to an entirely different project: few-shot *image*-diffusion-model adaptation on CIFAR-100/GMM2D, structured as a "three-way hand-off" package to team members named Jinjian/Yu Cao/Jing Peng. I confirmed this isn't a naming collision by checking file trees (`domains/cifar100.py`, `diffusion/schedule.py`, `episodes/`) and by grepping for SDE terms — the one "drift" hit was the English word in an unrelated doc comment about image statistics. **None of the 27 items are touched by these branches.**

Separately, `origin/main`'s Sep 4 commits (`hot275`) were an accidental upload of files from that same unrelated project (`domains/`, `diffusion/`, `config/`, `diagnostics/`, `artifacts/`, `adaptation/`), immediately followed by six "Delete X directory" cleanup commits. **That cleanup did not just remove the accidental files — it deleted the project's real, pre-existing `adaptation/` and `config/` directories along with them.** At the time this report was written, `main` HEAD was broken: `baselines/*.py`, `training/train_meta.py`, `data_gen/*.py`, `glucose/*.py`, and `simulators/*.py` all still did `from config.base_config import cfg` / `from adaptation.gated_finetuning_regularized import ...`, and those modules no longer existed in the tree.

**Update (resolved):** this was fixed the same day, shortly after this report was drafted — commit `5f20349` ("Restore adaptation/ and config/ accidentally deleted in Sep 4 cleanup", 2026-09-16 11:57, ~39 minutes after this report) restores both directories on `origin/main`. Confirmed current: `git ls-tree origin/main -- adaptation config` shows both present. No action needed on this point anymore.

Given this, **all 27 items are unaddressed by any recent push**. The table below also notes where the *pre-existing* (pre-submission, already-in-paper) code on main has something superficially related, since partial matches count per your instructions — but these predate the professor's list and are not responses to it.

| # | Task (short name) | Status | Branch + commit | Notes |
|---|---|---|---|---|
| 1 | Factorised zf/zg latents | Not started | — | `models/neural_sde.py`/`encoder.py` use one shared `z` for both drift and diffusion nets. No factorisation anywhere. |
| 2 | Euler-transition NLL objective | Not started | — | Existing loss is rollout MSE + a Gaussian NLL only on the *final* MC-rollout distribution (`adaptation/gated_inference.py`, pre-existing on main at `a85d525`), not a per-step Euler-Maruyama transition NLL. |
| 3 | Learned af/ag controller | Not started | Pre-existing scalar gate: main `ae5bf99`/`a85d525` (merged) | Current mechanism is exactly the "scalar heuristic gate" task 3 says to remove: `g = sigmoid(GATE_ALPHA*(GATE_TAU-d_norm))` in `deepmind/gated_finetuning_regularized_dm.py` / `adaptation/gated_finetuning_regularized.py`. No learned (af,ag) controller exists. |
| 4 | Meta-learned adaptation rates/reg | Not started | — | `LR_Z`, `BETA_REG`, `ADAPT_STEPS` are fixed global constants, not learned or support-conditioned. |
| 5 | Large factorial drift/diffusion benchmark | Not started | — | `sde_basis/basis_functions.py` has one generic basis family (8 drift terms, 3 diffusion terms) with sampled coefficients — not 10–20 named drift × 5–10 diffusion mechanisms. |
| 6 | Compositional generalisation holdout | Not started | — | No family labels exist to construct a drift×diffusion holdout matrix from. |
| 7 | Decisive selective-adaptation experiment (af>ag / ag>af) | Not started | — | Depends on task 3's controller, which doesn't exist; nothing tests differential drift-vs-diffusion adaptation. |
| 8 | Statistical power (seeds, 100+ tasks, CIs) | Partial (pre-existing, falls short) | main, pre-existing (`51a8ec0`, evaluation/regime_switch_experiment.py, final_comparison.py) | `N_SEEDS=5` and CI-plotting infra exist; `methodology.txt` §10.3 shows only ~30 test tasks/regime, well under the requested 100+. |
| 9 | Behavioural dynamics geometry d_dyn | Not started | — | `evaluation/proximity_analysis.py` measures L2 distance in flattened parameter space (θ), not functional drift/diffusion output distance. |
| 10 | Disentanglement cross-correlation + probes | Not started | — | `evaluation/latent_retrieval.py` compares single-z neighbours to θ-space neighbours; no zf/zg split, no drift/diffusion parameter probes. |
| 11 | Latent interpolation/intervention/swap | Partial (pre-existing, single-z only) | main, pre-existing (`a85d525`), evaluation/latent_interpolation.py | Does slerp interpolation between two tasks' single `z` and simulates trajectories — same spirit as task 11 but on an undivided latent, no zf/zg swap test. |
| 12 | Broad named-family training + leave-one-out | Not started | — | No OU/GBM/Duffing/pendulum/Lotka-Volterra/FitzHugh-Nagumo/double-well families exist; only the generic basis family. |
| 13 | Cross-family OOD benchmark | Not started | — | Same generic-basis-only limitation as #12; no K-shot sweep across named unseen families. |
| 14 | Strengthened external baselines | Partial (pre-existing, no HP search) | main, pre-existing (baselines/) | MAML/GRU/scratch/transfer/persistence baselines exist with a documented fairness audit (`methodology.txt` §9), but no serious per-baseline hyperparameter search and no Seifner/GP-SDE-ID/SINDy/oracle baselines. |
| 15 | Iso-compute comparison | Partial (pre-existing, narrow) | main, pre-existing, `methodology.txt` §9 | Adaptation steps and support size are already equalized across baselines, but no FLOPs/wall-clock-matched plots. |
| 16 | Scaling laws over Ntasks | Not started | — | `generate_meta_params.py` uses a fixed `n_train_thetas=150`; no sweep. |
| 17 | Support-efficiency curves (K sweep) | Partial (pre-existing, marked stale) | main, pre-existing, `deepmind/few_shot_sweep.py`, `evaluation/sweep_adaptation_budget.py` | Sweeps `N_SHOTS=[1,2,5,10]` (flagged "stale" in `methodology.txt` §3.3) and adaptation-step budgets separately; not the requested full K×method matrix. |
| 18 | Observation robustness (noise/missing/irregular) | Not started | — | No such factorial experiment exists; only context-length randomization during meta-training. |
| 19 | Rebuilt DMCS continuous-control experiment | Partial (pre-existing baseline version) | main, pre-existing (deepmind/) | `dm_wrapper.py`, `benchmark_manager.py`, `train_manager.py` already run DMCS reacher/finger/cheetah tasks, but not "rebuilt" per the screening/independent-randomization criteria in task 19. |
| 20 | Selective adaptation on DMCS interventions | Not started (mechanism exists, test doesn't) | main, pre-existing | `dm_wrapper.GeneralizedDMControl.set_physics()` already supports mass/friction scaling, but since no af/ag controller exists (#3), no differential-response test is possible. |
| 21 | Definitive architecture ablation | Partial (pre-existing, much narrower) | main, pre-existing, `adaptation/ablation_gate_and_latent.py` (recovered at `a85d525`; directory now deleted from HEAD, see caveat above) | Ablates gate on/adaptive/off and latent dim {8,16,32} only — far short of the requested 10-way ladder. |
| 22 | Objective ablations (λ sweep, NLL vs rollout) | Not started | — | No λ sweep, no rollout-vs-likelihood ablation; metrics are aggregate MSE only, never split into drift/diffusion error. |
| 23 | Latent-capacity/controller-capacity sweep | Partial (pre-existing, single-z only) | main, pre-existing, `evaluation/zdim_sweep.py` / `train_zdim_sweep.py` | Sweeps `z_dim ∈ {4,16,64}` on the single undivided latent — not zf/zg factorised capacity, no controller-capacity axis. |
| 24 | Oracle factorisation baseline | Not started | — | No oracle using true drift/diffusion labels or true θ exists. |
| 25 | Paper rewrite around new framing | Not started | — | `paper_text.txt` is still framed around "Model A/B/C" single-z adaptation. |
| 26 | Four decisive main-paper figures | Not started | — | Existing figures (hero curve, gate calibration, proximity/manifold distance, regime-switch) don't match the four requested (method diagram, selective adaptation, compositional generalisation, cross-family/geometry). |
| 27 | Reviewer-proofing clean-up | Not started | — | Confirmed still needed: "Model A/B/C" naming is used throughout `paper_text.txt`; `methodology.txt` itself separately documents reproducibility gaps (e.g. hardcoded benchmark numbers in `deepmind/plot_benchmark_results.py`) that haven't been fixed in the paper. |

**Bottom line:** 0/27 items are addressed by the Sep 4–13 activity. What partial pre-existing infrastructure exists (items 8, 11, 14, 15, 17, 19, 21, 23) is leftover baseline-paper code from before/around the May 7 submission, already merged into `main` — and part of it (the `adaptation/` ablation code, item 21) was briefly inaccessible on `main` HEAD due to the accidental-deletion bug described above (**now resolved** by `5f20349`, see the update note above). Nothing on `jinjian`, `yu-cao`, `meta-diffusion-clean`, or `jing-peng` is relevant — you may want to check with whoever pushed those (`jiangjinjian232517`, `Jing`) to confirm this repo is the wrong place for that work. (Note: this audit predates and is unrelated to item 5's factorial benchmark work below/elsewhere in this branch — it's a standalone branch/task-list audit, included here only because it was an uncommitted deliverable sitting in the working directory.)
